import turtle

t = turtle.Turtle()
turtle.title("Design")

def star():
    length = 200
    t.color("blue")
    for i in range(80):
        t.speed(10)
        t.rt(8)
        length -= 3
        for i in range(5):
            t.rt(144)
            t.fd(length)

star()
turtle.mainloop()
